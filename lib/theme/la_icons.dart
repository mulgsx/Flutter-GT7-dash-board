import 'package:flutter/material.dart';

/// Lap Analyzer画面共通で使うアイコンの定義。差し替えるときはここだけ直せばよい
/// Shared icon definitions for the Lap Analyzer screens — change the icon in
/// one place here instead of hunting through every screen.
class LAIcons {
  LAIcons._();

  static const IconData menu = Icons.menu;
  static const IconData success = Icons.check_circle;
}
